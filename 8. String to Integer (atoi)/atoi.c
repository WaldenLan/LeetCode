#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int int_parser(char a)
{
    int result = a - 48;
    return result;
}

int myAtoi(char *str) 
{
    int size = strlen(str);
    int result = 0;
    char *num
    
    for (int i = size-1; i >= 0; i--)
    {
        if (str[i])
    }

// Exception case:
    return 0;
}

// ASCii => Integer
// 48~57 => 0~9

int main(int argc, char const *argv[])
{
    char str[] = "12";
    return 0;
}